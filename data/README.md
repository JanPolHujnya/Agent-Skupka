Эта папка только на машине, в git не попадает.

- `deals.jsonl` — журнал сделок бота
- `state.json` — сессии мастеров Telegram
- `bot.log` — лог

Таблица Google Sheets — отдельный источник правды. Сюда её не выгружать.
